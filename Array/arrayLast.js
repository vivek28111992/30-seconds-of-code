// Array Last
// Returns the last element in an array.
// Use arr.length - 1 to compute the index of the last element of the given array & returning it.

const last = arr => arr[arr.length - 1];
const result = last([1, 3, 4]);
console.log(result);